# Copy to .env for Docker Compose, then use strong local-only values.
MYSQL_ROOT_PASSWORD=change-me

# Export these before starting the corresponding Spring services.
MYSQL_PASSWORD=change-me
NEO4J_PASSWORD=change-me
NEO4J_URI=bolt://localhost:7687
CONFIG_REPOSITORY_URI=https://github.com/your-org/config-server.git
